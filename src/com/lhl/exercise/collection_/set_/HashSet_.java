package com.lhl.exercise.collection_.set_;

/**
 * @author WIFI连接超时
 * @version 1.0
 * Create Time: 2023/12/6_20:55
 */
public class HashSet_ {
    /*
        1. hashSet() 构造器走的源码本质是 hashMap()
        2. hashSet 可以存放 null， 但是只能有一个，相同对象只能放一个
        3. 不保证存放顺序与取出顺序一致，却决于hash后，再确定索引结果
     */
}
